extends layout

block content
	each review in reviews
		p
			a(href="/reviews/" + review.id)= review.name
		p= review.placeType
		p= review.stars
		p
			a(id="delete" href="/reviews/" + review.id)= "supprimer"
